# Vulkan 版に同梱する最小限の Python パッケージ（scripts/prepare-vulkan-bundle-windows.ps1 がビルド時に入れる）。
# 文字起こし・話者分離は ggml エンジン、校正は llama-server なので、torch / faster-whisper / pyannote は入れない。
# 用途:
#   requests          校正 CLI（proofread_llm_cli.py / overall_proofread_cli.py）から loopback の llama-server へ送る
#   huggingface-hub   Gemma 4 GGUF の取得（download_gemma_gguf_cli.py）
#   msoffcrypto-tool  パスワード付き Word / Excel 保存（encrypt_office_cli.py）
#   pyzipper          パスワード付き ZIP 保存（encrypt_office_cli.py）
# Do not add PyAV (av) or imageio-ffmpeg here.
requests>=2.32,<3
huggingface-hub<1.0
tqdm>=4.64,<5
msoffcrypto-tool>=3.3
pyzipper>=0.3.6
